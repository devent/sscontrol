unix(chownCommand, owner, ownerGroup, files, recursive) ::= <<
#!/bin/sh
<files:{file|<chownCommand> <unixRecursive(recursive)> <owner>.<ownerGroup> <file><\n>}>

>>

unixRecursive(recursive) ::= <%
<if(recursive)>-R<endif>
%>
